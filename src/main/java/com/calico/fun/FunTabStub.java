package com.calico.fun;

/**
 * Phase 1 Fun tab stub — intentionally a no-op.
 * <p>
 * The Fun tab must not expose settings or behavior in Phase 1.
 * Later phases may replace this with real Fun features.
 */
public final class FunTabStub {
    private FunTabStub() {
    }

    /** No-op placeholder so the Fun package exists without behavior. */
    public static void noop() {
        // intentionally empty
    }
}
